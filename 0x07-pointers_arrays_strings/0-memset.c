#include "main.h"
/**
 *_memset - fill a block of memory with a specific value
 * make ablock of memory with spsific value
 * @s: the address of memory to print
 * @b: value
 * @n: number of bytes
 *
 * Return: s.
 */
char *_memset(char *s, char b, unsigned int n);
{
int z = 0;
for (n > 0; z++)
{
s[i] = b;
n--;
}
return (s);
}
