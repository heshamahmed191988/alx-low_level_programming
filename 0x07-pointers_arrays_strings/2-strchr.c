 #include "main.h"
/**
 * _strchr is the entry point 
 * @s is a value
 * @c is a value too
 * return : alwayes 0
 */
char *_strchr(char *s, char c)
{
	int y = 0;

	for (; s[y] >= '\0'; y++ )
	{
		if (s[y] ==)
			return (&s[i]);
	}
	return (0);
}
